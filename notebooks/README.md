# 实验与可视化

放探索性实验、指标曲线与失败样本可视化的 notebook，文件名建议 `NN_主题.ipynb`（例如 `01_m2_enhance_compare.ipynb`）。

约定：
- notebook 只做分析与看图，可复现的结论请回填到模块实现或 `docs/methods/`；
- 不在 notebook 里写死数据集绝对路径，统一通过 `configs/` 读取；
- 输出图如需长期保留，放到 `docs/figures/` 或本目录的 `assets/` 下。
