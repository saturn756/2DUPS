# 架构设计与规范

本目录定义「每个模块负责什么、模块之间传什么、有哪些硬约束」，**不涉及具体实现**（算法、模型、阈值、数据集版本都在实施阶段填入）。

| 文件 | 内容 |
|---|---|
| `01_系统架构.md` | 目标与范围、架构风格、六模块职责总览、数据流与评测与验证 |
| `02_模块规范.md` | 每个模块：职责、边界（不做什么）、输入、输出、约束、验收维度 |
| `05_实施规划.md` | 骨架与实现的边界、填充顺序、选型流程、评测体系、待填清单 |
| [`../dataflow/03_数据流与连接逻辑.md`](../dataflow/03_数据流与连接逻辑.md) | 模块之间有哪些边、处理单元与触发条件、对齐与聚合、失败传播、连接级不变量 |
| [`../interface/04_接口规范.md`](../interface/04_接口规范.md) | 由连接推导的 9 个接口：字段、不变量、降级语义、版本与变更流程 |
| [`../methods/`](../methods/README.md) | 各模块方法与候选对比（随实现补充） |
| [`../figures/`](../figures/README.md) | 图 1-1 系统架构、图 1-2 数据流与连接图（PNG + SVG） |

## 阅读顺序

1. 先看 [图 1-1（全新设计）](../figures/图1-1_全新设计_2D感知系统架构.png)（模块功能与数据流），再看 [图 1-2](../figures/图1-2_数据流与连接图.png)（边与接口）；
2. `01_系统架构.md`：范围与模块划分依据；
3. [`03_数据流与连接逻辑.md`](../dataflow/03_数据流与连接逻辑.md)：模块之间怎么连、缺了会怎样；
4. 做实现时查 `02_模块规范.md` 与 [`04_接口规范.md`](../interface/04_接口规范.md)；
5. 排期与填坑看 `05_实施规划.md`。

## 设计原则

| 编号 | 原则 | 含义 |
|---|---|---|
| P1 | 接口先行，实现可替换 | 模块之间只通过接口通信；替换实现不得改变接口 |
| P2 | 只传结构化结果 | 不传递高维特征张量，避免下游与上游实现强绑定 |
| P3 | 失败必须显式 | 用状态字段表达「不可用 / 不可靠 / 降级」，不允许用默认值掩盖 |
| P4 | 可选输入缺失时降级 | 下游在缺少可选输入时给出降级结果，而不是猜测或崩溃 |
| P5 | 每模块可独立评测 | 每个模块有独立的验收维度与数据切片 |
| P6 | 语义与几何分离 | 语义结果与几何结果分开产出，互不替代 |

## 文档依赖与改动顺序

```
01_系统架构 ── 定模块与边界
     │
02_模块规范 ── 定每个模块的职责、输入输出与约束
     │
03_数据流与连接逻辑 ── 定边、顺序、对齐与失败传播   ← 连接变了先改这里
     │
04_接口规范 ── 由连接推导出字段与不变量
     │
05_实施规划 ── 定怎么把实现填进来
```

改动顺序规则：**模块职责变 → 先改 01/02；连接变 → 先改 03；字段变 → 改 04**，图与下游材料在对应步骤后同步。

## 当前状态

- 已完成：骨架（模块职责、接口契约、约束、三条并列支路、评测与验证）。
- 待填（实施阶段）：各模块的具体实现、参数与阈值、数据集版本、评测基线数值，见 `05_实施规划.md` 的待填清单。
