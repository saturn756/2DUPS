# 2DUPS 项目依赖。Linux x86_64 / NVIDIA CUDA 12.8 基线；精确版本见 requirements-lock-linux-py311.txt。
# PyTorch CUDA wheel 不在普通 PyPI 索引，须显式添加官方索引。
--extra-index-url https://download.pytorch.org/whl/cu128

# ---- 基础 ----
numpy
opencv-python
PyYAML
tqdm

# ---- 数据与评测 ----
av                         # M1: MOV/H.264 流式解码及旋转元数据
pandas
openpyxl
pillow
matplotlib

# ---- M2 图像增强与去噪 ----
# 当前 M2 只使用 OpenCV

# ---- M3 特征检测与匹配 ----
# （SIFT/FLANN 由 opencv-python 提供；XFeat 对照实现按官方仓库引入）

# ---- M4 目标检测与跟踪 ----
torch==2.8.0+cu128
torchvision==0.23.0+cu128
ultralytics==8.3.184     # YOLO11n 检测适配器；不包含模型权重

# ---- M5 语义分割 ----
# （PIDNet 以官方仓库为准，按需引入）

# ---- M6 拓扑推理 ----
# （规则化实现，暂无额外依赖）
