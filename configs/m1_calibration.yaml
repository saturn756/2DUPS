# M1 通用模块尚未接通；BDD100K DataLoader 独立入口见 scripts/run_dataloader.py。
module: M1
name: 输入接入与标定状态管理
status: partial

impl:
  loader: bdd100k_video_loader
  calibration: deferred            # 无已验证内参；不得输出 rectified

io:
  inputs: [image_sequence, camera_params_optional, dataset_manifest]
  outputs: [CalibratedFrame, DatasetManifest]     # I2 / I1

params:
  calibration_status: [valid, missing, stale]

producer_ref: m1.bdd100k_video_loader
