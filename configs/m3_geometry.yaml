# M3 帧间几何估计（骨架）
module: M3
name: 帧间几何估计
status: planned

impl:
  candidate: sift_flann_ransac     # 尚未实现，不是可运行选定方法

io:
  inputs: [ImageVariantSet]        # 图像对（相邻两帧，同一 variant_id）
  outputs: [MatchResult]           # I4（可选边，供 M4 跟踪与 M6）

# 待填（阶段四）：特征与匹配方式、外点剔除策略、可用性判据与阈值
params:
  pair: adjacent                   # pair(t-1, t)，结果只用于 t 帧及之后
  ratio_test: 0.8
  ransac: {}                       # 尚未设定
  status_levels: [ok, unreliable, failed]
  min_inlier_ratio: null           # 待填
  transform_semantics: image_plane_transform
  use_metric_geometry: false

producer_ref: null                 # 实现后填写
